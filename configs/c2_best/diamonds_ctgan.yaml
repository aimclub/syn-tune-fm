# @package _global_
# Optuna best trial 11, multirun/2026-05-20/12-14-20/3
defaults:
  - override /dataset: diamonds
  - override /generator: ctgan

generator:
  params:
    epochs: 1000
    batch_size: 500
    generator_lr: 0.0001
    discriminator_lr: 0.0001
    discriminator_steps: 5
    generator_dim: [100, 100, 100, 100]
    discriminator_dim: [100, 100, 100, 100]
    pac: 10
