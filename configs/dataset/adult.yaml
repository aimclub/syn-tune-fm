# Adult (Census): income >50K — binary, strong class imbalance, many categorical features (education, occupation, race, gender, etc.)
name: openml
params:
  dataset_id: 179
  target_column: "class"
  test_size: 0.2
